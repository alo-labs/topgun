---
phase: 06-caching-state-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/topgun-tools.cjs
  - tests/topgun-tools-cache.test.cjs
autonomous: true
requirements: [REQ-23]

must_haves:
  truths:
    - "Cached audit older than 24h returns hit:false with stale:true"
    - "Cache invalidated when upstream updated_at or etag differs from cached value"
    - "Stale cache serves data with warning message including age"
    - "--force-audit flag bypasses cache entirely"
    - "topgun-lock.json written to ~/.topgun/ after successful audit cache write"
  artifacts:
    - path: "bin/topgun-tools.cjs"
      provides: "Enhanced cache-lookup with TTL, etag/updated_at invalidation, lock file write"
      contains: "cache-invalidate"
    - path: "tests/topgun-tools-cache.test.cjs"
      provides: "Unit tests for cache TTL, invalidation, stale-with-warning, force-audit, lock file"
  key_links:
    - from: "bin/topgun-tools.cjs (cache-lookup)"
      to: "~/.topgun/audit-cache/{sha}.json"
      via: "fs.readFileSync + age + etag comparison"
      pattern: "updated_at|etag"
---

<objective>
Harden the audit cache in topgun-tools.cjs to production-grade: TTL enforcement, upstream invalidation via updated_at/etag, stale-with-warning serving, --force-audit bypass, and topgun-lock.json for reproducibility.

Purpose: REQ-23 requires cached audits with 24h TTL, invalidation on upstream changes, and cache bypass. This makes the cache trustworthy for offline and resume scenarios.
Output: Enhanced topgun-tools.cjs cache commands, topgun-lock.json write logic, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@bin/topgun-tools.cjs

<interfaces>
<!-- Existing cache-lookup command (lines 83-93 of topgun-tools.cjs): -->
```javascript
case 'cache-lookup': {
  const sha = args[0];
  if (!sha) { output({ hit: false }); break; }
  const cachePath = path.join(TOPGUN_HOME, 'audit-cache', `${sha}.json`);
  if (!fs.existsSync(cachePath)) { output({ hit: false }); break; }
  const cached = JSON.parse(fs.readFileSync(cachePath, 'utf8'));
  const age = Date.now() - new Date(cached.cached_at).getTime();
  const ttl = 24 * 60 * 60 * 1000;
  output({ hit: age < ttl, stale: age >= ttl, age_hours: Math.round(age / 3600000), data: cached });
  break;
}

// Existing cache-write command (lines 95-113):
case 'cache-write': {
  const sha = args[0];
  const jsonData = args.slice(1).join(' ');
  // ... parses JSON, adds cached_at, writes to audit-cache/{sha}.json
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhance cache-lookup with etag/updated_at invalidation and stale-with-warning</name>
  <files>bin/topgun-tools.cjs, tests/topgun-tools-cache.test.cjs</files>
  <behavior>
    - Test 1: cache-lookup with fresh cache (age < 24h, matching etag) returns {hit: true, stale: false}
    - Test 2: cache-lookup with expired cache (age > 24h) returns {hit: false, stale: true, warning: "Audit cached N hours ago..."}
    - Test 3: cache-lookup with --force flag returns {hit: false, forced: true} regardless of age
    - Test 4: cache-lookup with mismatched upstream_etag returns {hit: false, invalidated: true, reason: "upstream changed"}
    - Test 5: cache-lookup with mismatched upstream_updated_at returns {hit: false, invalidated: true}
    - Test 6: stale cache returns data in response with warning string containing age in hours
  </behavior>
  <action>
    Modify cache-lookup in topgun-tools.cjs:
    1. Accept optional args: `cache-lookup <sha> [--force] [--upstream-etag <etag>] [--upstream-updated-at <iso>]`
    2. Parse --force flag from args array. If present, return {hit: false, forced: true} immediately.
    3. After reading cached JSON, check for `cached.etag` and `cached.updated_at` fields. If --upstream-etag provided and differs from cached.etag, return {hit: false, invalidated: true, reason: "upstream etag changed"}.
    4. Same for --upstream-updated-at vs cached.updated_at.
    5. Keep existing TTL logic (24h). When stale (age >= ttl), return {hit: false, stale: true, age_hours: N, warning: `Audit cached ${N} hours ago -- use --force-audit to refresh`, data: cached}.
    6. When fresh and valid, return {hit: true, stale: false, age_hours: N, data: cached}.

    Modify cache-write to accept optional etag and updated_at:
    `cache-write <sha> <json> [--etag <etag>] [--updated-at <iso>]`
    Store etag and updated_at in the cached JSON alongside cached_at.

    Create tests/topgun-tools-cache.test.cjs using Node's built-in test runner (node:test + node:assert). Tests should create temp dirs, write mock cache files, and invoke the cache-lookup/cache-write functions directly by requiring topgun-tools logic or by spawning the CLI. Use temp directories to avoid polluting ~/.topgun/.
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/TopGun && node --test tests/topgun-tools-cache.test.cjs</automated>
  </verify>
  <done>All 6 behavior tests pass. cache-lookup respects TTL, etag, updated_at, and --force. Stale responses include warning text.</done>
</task>

<task type="auto">
  <name>Task 2: Add topgun-lock.json write and cache-invalidate command</name>
  <files>bin/topgun-tools.cjs, tests/topgun-tools-cache.test.cjs</files>
  <action>
    Add new command `lock-write` to topgun-tools.cjs:
    `lock-write <json>` — writes the provided JSON to ~/.topgun/topgun-lock.json. The lock file records: {locked_at, audits: [{sha, cached_at, etag, source_registry}], topgun_version: "1.0"}.

    Add new command `lock-read`:
    Reads and returns ~/.topgun/topgun-lock.json, or {exists: false} if missing.

    Add new command `cache-invalidate`:
    `cache-invalidate <sha>` — deletes ~/.topgun/audit-cache/{sha}.json if it exists. Returns {status: "ok", deleted: true} or {status: "ok", deleted: false} if not found.

    `cache-invalidate --all` — deletes all files in ~/.topgun/audit-cache/. Returns {status: "ok", count: N}.

    Update the help text in the default case to include new commands.

    Add tests for lock-write, lock-read, cache-invalidate, and cache-invalidate --all.
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/TopGun && node --test tests/topgun-tools-cache.test.cjs</automated>
  </verify>
  <done>lock-write creates valid topgun-lock.json. lock-read returns it. cache-invalidate removes single or all cached audits. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args to topgun-tools.cjs | Untrusted input from orchestrator or user flags |
| Cache files on disk | Could be tampered with between runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | audit-cache/{sha}.json | mitigate | Validate JSON parse; if malformed, treat as cache miss (not crash) |
| T-06-02 | Spoofing | --upstream-etag arg | accept | Etag comes from orchestrator which already validated registry response |
| T-06-03 | Information Disclosure | topgun-lock.json | mitigate | Lock file contains only SHAs and timestamps, no secrets or tokens |
</threat_model>

<verification>
- `node bin/topgun-tools.cjs cache-lookup <test-sha>` returns correct hit/stale/invalidated status
- `node bin/topgun-tools.cjs cache-lookup <sha> --force` always returns {hit: false, forced: true}
- `node bin/topgun-tools.cjs lock-write '{"audits":[]}'` creates ~/.topgun/topgun-lock.json
- `node bin/topgun-tools.cjs cache-invalidate --all` clears audit-cache directory
- All unit tests pass: `node --test tests/topgun-tools-cache.test.cjs`
</verification>

<success_criteria>
- Cached audits respect 24h TTL with stale-with-warning
- Upstream etag/updated_at mismatch invalidates cache
- --force bypasses cache entirely
- topgun-lock.json written for reproducibility
- cache-invalidate clears single or all cached audits
</success_criteria>

<output>
After completion, create `.planning/phases/06-caching-state-resilience/06-01-SUMMARY.md`
</output>
