---
phase: 06-caching-state-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/topgun/SKILL.md
  - tests/orchestrator-resume.test.cjs
autonomous: true
requirements: [REQ-24, REQ-26]

must_haves:
  truths:
    - "Resume check verifies stage output files exist on disk, not just state flag"
    - "--offline flag uses cache-only mode and errors clearly if no cache"
    - "--reset flag clears state.json and restarts pipeline from scratch"
    - "state.json schema covers all stages: find, compare, secure, approval, install, complete"
  artifacts:
    - path: "skills/topgun/SKILL.md"
      provides: "Hardened resume logic, --offline flag, --reset flag"
      contains: "--offline"
    - path: "tests/orchestrator-resume.test.cjs"
      provides: "Tests for resume verification, --offline, --reset"
  key_links:
    - from: "skills/topgun/SKILL.md (Step 2)"
      to: "~/.topgun/state.json"
      via: "state-read + file existence check"
      pattern: "fs.existsSync|found-skills|comparison|audit"
---

<objective>
Harden stage resumption in the orchestrator SKILL.md: verify output files actually exist before skipping stages, add --offline cache-only mode, add --reset to clear state, and validate state.json schema completeness.

Purpose: REQ-24 requires reliable resume-from-last-stage. REQ-26 requires --offline mode. Without file existence verification, resume can skip to a stage whose input doesn't exist, causing silent failures.
Output: Updated orchestrator SKILL.md with hardened resume, --offline, --reset. Unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@skills/topgun/SKILL.md
@bin/topgun-tools.cjs

<interfaces>
<!-- Current state.json schema from topgun-tools.cjs (lines 160-175): -->
```javascript
'state': {
  type: 'object',
  properties: {
    current_stage: { type: ['string', 'null'], enum: ['find', 'compare', 'secure', 'approve', 'install', 'complete', null] },
    run_id: { type: ['string', 'null'] },
    started_at: { type: ['string', 'null'], format: 'date-time' },
    task_description: { type: ['string', 'null'] },
    registries: { type: ['array', 'null'], items: { type: 'string' } },
    found_skills_path: { type: ['string', 'null'] },
    comparison_path: { type: ['string', 'null'] },
    audit_path: { type: ['string', 'null'] },
    last_completed_stage: { type: ['string', 'null'] }
  },
  required: ['current_stage', 'run_id']
}
```

<!-- Current resume logic in SKILL.md (Step 2, lines 49-57): -->
```markdown
## Step 2: Resume Check
Read the state from Step 0. Check `last_completed_stage`:
- If `null` or missing -> start from FindSkills (Step 3)
- If `find` -> skip to CompareSkills (Step 4)
- If `compare` -> skip to SecureSkills (Step 5)
- If `secure` -> go to approval gate (Step 6)
- If `approve` -> skip to InstallSkills (Step 7)
- If `install` or `complete` -> inform user pipeline already completed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden resume logic with output file verification, --offline, --reset</name>
  <files>skills/topgun/SKILL.md</files>
  <action>
    Update SKILL.md Step 1 (Parse Input) to also detect:
    - `--offline` flag: set a variable offline=true. When offline, all sub-agent dispatches must pass "--offline" context. Before dispatching FindSkills, check if cached results exist for this query. If no cache exists, output error: "No cached results available for this query. Run online first." and STOP.
    - `--reset` flag: before any other logic, run `node "$CLAUDE_PLUGIN_ROOT/bin/topgun-tools.cjs" state-write current_stage null` and `state-write last_completed_stage null` and `state-write run_id null`. Output "State cleared. Starting fresh pipeline." Then proceed normally.
    - `--force-audit` flag: pass through to SecureSkills sub-agent prompt so it calls cache-lookup with --force.

    Update SKILL.md Step 2 (Resume Check) to verify output files exist before trusting state:
    ```
    - If `find` -> verify found_skills_path exists on disk:
        node -e "process.exit(require('fs').existsSync('${found_skills_path}') ? 0 : 1)"
      If file missing, warn "Found-skills output missing, re-running FindSkills" and start from Step 3.
    - If `compare` -> verify comparison_path exists on disk. If missing, check found_skills_path. Resume from earliest missing stage.
    - If `secure` -> verify audit_path exists on disk. If missing, resume from SecureSkills.
    - If `approve` -> verify audit_path exists. If missing, resume from SecureSkills.
    ```

    Update each sub-agent dispatch (Steps 3-7) to write the output file path to state after completion:
    - After FindSkills: `state-write found_skills_path <path>`
    - After CompareSkills: `state-write comparison_path <path>`
    - After SecureSkills: `state-write audit_path <path>`

    Add Step 1.5: Offline Check
    If --offline is set:
    - For FindSkills: check ~/.topgun/cache/ or found-skills-*.json files. If none, error clearly.
    - For SecureSkills: check audit-cache. If no cached audit for the winning skill's SHA, error: "No cached audit available. Cannot proceed offline."
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/TopGun && grep -c "\-\-offline\|\-\-reset\|existsSync\|file missing" skills/topgun/SKILL.md</automated>
  </verify>
  <done>SKILL.md resume logic verifies file existence. --offline errors if no cache. --reset clears state. --force-audit passes through to SecureSkills.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: State validation tests and --reset/--offline integration tests</name>
  <files>tests/orchestrator-resume.test.cjs</files>
  <behavior>
    - Test 1: state-write + state-read roundtrip for all stage values (find, compare, secure, approve, install, complete)
    - Test 2: state.json created by init contains all required fields from schema
    - Test 3: state-write current_stage null + last_completed_stage null effectively resets (simulating --reset)
    - Test 4: SKILL.md contains --offline handling instructions
    - Test 5: SKILL.md contains file existence verification for each resume stage
  </behavior>
  <action>
    Create tests/orchestrator-resume.test.cjs using node:test + node:assert.

    Test state roundtrip: use a temp TOPGUN_HOME, run topgun-tools.cjs init, then state-write each stage value, state-read and verify.

    Test schema completeness: run `topgun-tools.cjs schemas state` and verify all required fields present, verify enum includes all 6 stages.

    Test reset simulation: write state with last_completed_stage="compare", then write null to both current_stage and last_completed_stage, read back and verify both are null.

    Test SKILL.md content: read skills/topgun/SKILL.md and assert it contains "--offline", "--reset", "existsSync" or equivalent file check pattern, and all stage names.
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/TopGun && node --test tests/orchestrator-resume.test.cjs</automated>
  </verify>
  <done>All state roundtrip tests pass. Schema covers all stages. SKILL.md contains --offline, --reset, and file verification logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state.json on disk | Could be manually edited or corrupted between runs |
| --offline flag | User trusts cached data which may be outdated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | state.json | mitigate | Validate stage values against known enum before trusting; invalid value = restart from scratch |
| T-06-05 | Denial of Service | --offline with no cache | mitigate | Clear error message explaining no cache exists, not a silent failure or hang |
| T-06-06 | Elevation of Privilege | Resume skipping approval gate | mitigate | File verification ensures audit_path exists before resuming past secure stage; approval stage always re-prompts user |
</threat_model>

<verification>
- Modified SKILL.md contains --offline, --reset, --force-audit flag handling
- Resume logic checks file existence before skipping stages
- `node --test tests/orchestrator-resume.test.cjs` passes
- State schema enum includes: find, compare, secure, approve, install, complete, null
</verification>

<success_criteria>
- Interrupted pipeline resumes correctly only when output files exist
- --offline errors clearly when no cache available
- --reset clears state and starts fresh
- --force-audit bypasses audit cache
</success_criteria>

<output>
After completion, create `.planning/phases/06-caching-state-resilience/06-02-SUMMARY.md`
</output>
