---
phase: 03-compareskills-multi-factor-ranking
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/topgun-comparator.md
  - skills/compare-skills/SKILL.md
autonomous: true
requirements: [REQ-09, NFR-01]

must_haves:
  truths:
    - "All registry metadata fields are wrapped in structural envelope before any scoring"
    - "SKILL.md content with base64 blobs is rejected with logged reason"
    - "SKILL.md content with Unicode > U+2000 is rejected with logged reason"
    - "SKILL.md content with zero-width characters is rejected with logged reason"
  artifacts:
    - path: "agents/topgun-comparator.md"
      provides: "Structural envelope logic and pre-filter steps"
      contains: "structural envelope"
    - path: "skills/compare-skills/SKILL.md"
      provides: "Pre-filter documentation and envelope step"
      contains: "Pre-filter"
  key_links:
    - from: "agents/topgun-comparator.md"
      to: "found-skills-{hash}.json"
      via: "reads normalized results from FindSkills output"
      pattern: "found-skills"
---

<objective>
Implement structural envelope enforcement and pre-filters for the CompareSkills agent.

Purpose: REQ-09 and NFR-01 require all registry metadata to pass through the structural envelope before agent context injection. Pre-filters catch base64 blobs, high-Unicode, and zero-width characters before any scoring occurs.

Output: Updated agents/topgun-comparator.md and skills/compare-skills/SKILL.md with envelope + pre-filter logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-findskills-registry-adapters/02-03-SUMMARY.md

<interfaces>
<!-- FindSkills unified output schema (from 02-03-SUMMARY) -->
Each result in found-skills-{hash}.json has these fields:
  name, description, source_registry, install_count, stars,
  security_score, last_updated, content_sha, install_url, raw_metadata
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural envelope + pre-filters in topgun-comparator.md</name>
  <files>agents/topgun-comparator.md</files>
  <action>
Replace the "Current Implementation (Phase 1 Stub)" section in agents/topgun-comparator.md with full CompareSkills Phase 3 logic. Add the following steps before any scoring:

**Step 1 — Load found-skills output:**
Read state to get found-skills path:
```bash
node "$CLAUDE_PLUGIN_ROOT/bin/topgun-tools.cjs" state-read
```
Read the found-skills-{hash}.json file.

**Step 2 — Pre-filter candidates:**
For each candidate, inspect `raw_metadata` and any fetched SKILL.md content. Reject (remove from candidate list, log reason) if:
- Contains base64 blobs: regex `/[A-Za-z0-9+\/]{100,}={0,2}/` in any string field
- Contains Unicode codepoints > U+2000: regex `/[\u2001-\uFFFF]/` in any string field
- Contains zero-width characters: regex `/[\u200B-\u200F\u2028-\u202F\uFEFF]/` in any string field

Log each rejection: `"PRE-FILTER REJECT: {name} from {source_registry} — reason: {base64|high-unicode|zero-width}"`

If ALL candidates rejected, abort with error message.

**Step 3 — Structural envelope:**
Wrap every metadata string field (name, description, raw_metadata values) in the structural envelope pattern before any further processing:
```
<structural_envelope source="{source_registry}" field="{field_name}">
{field_value}
</structural_envelope>
```

This ensures no metadata field can break out of its designated context boundary. Numeric fields (install_count, stars, security_score) are NOT wrapped — only string fields.

Keep the `## COMPARE COMPLETE` marker at the end (will be finalized in Plan 03).
  </action>
  <verify>
    <automated>grep -c "structural_envelope\|structural envelope" agents/topgun-comparator.md | grep -v "^0$" && grep -c "PRE-FILTER\|pre-filter\|Pre-filter" agents/topgun-comparator.md | grep -v "^0$" && grep -c "base64\|zero-width\|U+2000\|\\\\u200B" agents/topgun-comparator.md | grep -v "^0$"</automated>
  </verify>
  <done>topgun-comparator.md contains structural envelope wrapping for all string metadata fields, pre-filter logic for base64/high-unicode/zero-width, rejection logging, and abort-on-all-rejected guard.</done>
</task>

<task type="auto">
  <name>Task 2: Update compare-skills SKILL.md with pre-filter and envelope documentation</name>
  <files>skills/compare-skills/SKILL.md</files>
  <action>
Replace the stub content in skills/compare-skills/SKILL.md. Keep the frontmatter. Replace everything after frontmatter with:

# CompareSkills

**Status:** Phase 3 — Step 1 (Envelope + Pre-filter)

## Overview

Evaluates skill candidates from FindSkills output across four scoring dimensions. Before any scoring, all metadata passes through structural envelope enforcement and pre-filters per REQ-09 and NFR-01.

## Pre-Filter Rules

Candidates are rejected before scoring if any string field contains:
1. **Base64 blobs** — sequences of 100+ base64 characters
2. **High Unicode** — codepoints above U+2000 (excluding standard punctuation)
3. **Zero-width characters** — U+200B through U+200F, U+2028 through U+202F, U+FEFF

Rejected candidates are logged with reason and excluded from scoring.

## Structural Envelope

All string metadata fields (name, description, raw_metadata values) are wrapped in `<structural_envelope>` tags with source registry and field name attributes before any agent context injection.

Numeric fields (install_count, stars, security_score) pass through unwrapped.

## Dispatch

This skill is dispatched by the topgun orchestrator via the topgun-comparator agent. Not normally invoked directly.
  </action>
  <verify>
    <automated>grep -c "Pre-Filter\|structural_envelope\|Structural Envelope" skills/compare-skills/SKILL.md | grep -v "^0$"</automated>
  </verify>
  <done>SKILL.md documents pre-filter rules, structural envelope pattern, and is no longer a stub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry metadata -> agent context | Untrusted string fields from external registries cross into agent processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | raw_metadata fields | mitigate | Structural envelope wraps all string fields, preventing context injection |
| T-03-02 | Tampering | SKILL.md content with encoded payloads | mitigate | Pre-filter rejects base64 blobs, high-unicode, zero-width chars |
| T-03-03 | Information Disclosure | rejection log messages | accept | Logs contain field name and reason only, no full content |
</threat_model>

<verification>
- grep for "structural_envelope" in agents/topgun-comparator.md returns matches
- grep for "PRE-FILTER" or "pre-filter" in agents/topgun-comparator.md returns matches
- grep for base64/unicode/zero-width detection patterns in agents/topgun-comparator.md returns matches
- skills/compare-skills/SKILL.md is no longer a stub
</verification>

<success_criteria>
- All registry metadata string fields wrapped in structural envelope before scoring
- Pre-filters detect and reject base64, high-unicode, and zero-width character content
- Rejections logged with candidate name, registry, and reason
- SKILL.md documents the envelope and pre-filter contract
</success_criteria>

<output>
After completion, create `.planning/phases/03-compareskills-multi-factor-ranking/03-01-SUMMARY.md`
</output>
