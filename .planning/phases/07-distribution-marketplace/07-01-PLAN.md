---
phase: 07-distribution-marketplace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - .claude-plugin/plugin.json
  - README.md
autonomous: true
requirements: [REQ-28, NFR-06]

must_haves:
  truths:
    - "marketplace.json exists with all required fields for Claude plugin marketplace"
    - "plugin.json has all fields needed for /plugin install"
    - "README.md documents installation, usage, and all CLI flags"
    - "autoUpdate is enabled in marketplace.json"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace listing metadata"
      contains: "autoUpdate"
    - path: "README.md"
      provides: "User-facing documentation"
      contains: "/plugin install alo-labs/topgun"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: ".claude-plugin/plugin.json"
      via: "version and name must match"
      pattern: "\"version\""
---

<objective>
Create marketplace.json for Claude plugin distribution, verify plugin.json completeness, and write the project README.md with installation and usage documentation.

Purpose: Enable TopGun to be distributed and discovered via the Claude plugin marketplace.
Output: marketplace.json, updated plugin.json, README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude-plugin/plugin.json

<interfaces>
From .claude-plugin/plugin.json:
```json
{
  "name": "topgun",
  "version": "1.0.0",
  "description": "Find, compare, security-audit, and install the best available skill for any job from 18+ registries.",
  "author": { "name": "Alo Labs", "url": "https://github.com/alo-labs/topgun" },
  "repository": "https://github.com/alo-labs/topgun",
  "license": "MIT",
  "skills": "./skills/"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketplace.json and verify plugin.json</name>
  <files>.claude-plugin/marketplace.json, .claude-plugin/plugin.json</files>
  <action>
Create `.claude-plugin/marketplace.json` with the following fields:
```json
{
  "name": "topgun",
  "displayName": "TopGun",
  "description": "Find, compare, security-audit, and install the best available skill for any job from 18+ registries.",
  "version": "1.0.0",
  "author": "alo-labs",
  "homepage": "https://github.com/alo-labs/topgun",
  "repository": "https://github.com/alo-labs/topgun",
  "license": "MIT",
  "tags": ["skills", "security", "audit", "registry", "marketplace", "install"],
  "autoUpdate": {
    "enabled": true,
    "channel": "stable",
    "checkInterval": "24h"
  },
  "minClaudeCodeVersion": "1.0.0",
  "entrypoint": "./skills/"
}
```

Verify plugin.json has all required fields for `/plugin install`. The current plugin.json is complete — confirm `name`, `version`, `description`, `author`, `repository`, `license`, and `skills` are present. No changes needed to plugin.json unless a field is missing.

Per REQ-28: marketplace.json enables Claude plugin system distribution.
Per NFR-06: maintain `.claude-plugin/` layout standard.
  </action>
  <verify>
    <automated>node -e "const m = require('./.claude-plugin/marketplace.json'); const required = ['name','version','description','author','homepage','tags','autoUpdate']; const missing = required.filter(k => !m[k]); if(missing.length) { console.error('Missing:', missing); process.exit(1); } if(!m.autoUpdate.enabled) { console.error('autoUpdate not enabled'); process.exit(1); } console.log('marketplace.json valid');"</automated>
  </verify>
  <done>marketplace.json exists with all required fields, autoUpdate enabled, version matches plugin.json</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with installation and usage docs</name>
  <files>README.md</files>
  <action>
Create `README.md` at project root with the following structure:

```markdown
# TopGun

Find, compare, security-audit, and install the best available skill for any job — searching 18+ registries automatically.

## Quick Start

### Install via Claude Plugin System

```
/plugin install alo-labs/topgun
```

### Install via skills.sh

```
npx skills add alo-labs/topgun
```

## Usage

```
/topgun "find a deployment skill"
```

TopGun will:
1. **Search** 18+ skill registries in parallel
2. **Compare** candidates across capability, security, popularity, and recency
3. **Audit** the top pick with Alo Labs Sentinel (2 clean passes required)
4. **Present** the audit manifest for your approval
5. **Install** the skill and display the audit trail

## CLI Flags

| Flag | Description |
|------|-------------|
| `--registries <list>` | Comma-separated list of registries to search (default: all) |
| `--offline` | Use cached results only — no network requests |
| `--reset` | Clear state and start fresh |
| `--force-audit` | Bypass audit cache and re-run Sentinel |

## How It Works

[Brief architecture description — orchestrator dispatches FindSkills, CompareSkills, SecureSkills, InstallSkills in sequence with security gating at every step.]

## License

MIT — Alo Labs
```

NOTE: Leave a placeholder section `## skills.sh` at the end for Plan 07-02 to fill in with skills.sh-specific content. Add a comment: `<!-- skills.sh section added by 07-02 -->`
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const r = fs.readFileSync('README.md','utf8'); const checks = ['/plugin install alo-labs/topgun','--registries','--offline','--reset','--force-audit','/topgun']; const missing = checks.filter(c => !r.includes(c)); if(missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('README.md valid');"</automated>
  </verify>
  <done>README.md exists with quick start, usage example, all 4 CLI flags documented, and placeholder for skills.sh section</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketplace.json → plugin system | Marketplace metadata consumed by Claude plugin installer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | marketplace.json | mitigate | Version field must match plugin.json; GitHub release tags provide integrity via signed commits |
| T-07-02 | S (Spoofing) | /plugin install | accept | Plugin system handles author verification; out of TopGun's control |
</threat_model>

<verification>
- marketplace.json passes JSON parse and has all required fields
- plugin.json unchanged or updated with any missing fields
- README.md contains install command, usage example, and all 4 flags
</verification>

<success_criteria>
- `node -e "require('./.claude-plugin/marketplace.json')"` succeeds
- README.md contains `/plugin install alo-labs/topgun` and all flag documentation
- autoUpdate.enabled is true in marketplace.json
</success_criteria>

<output>
After completion, create `.planning/phases/07-distribution-marketplace/07-01-SUMMARY.md`
</output>
