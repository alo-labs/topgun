---
phase: 07-distribution-marketplace
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - README.md
  - .claude-plugin/marketplace.json
autonomous: true
requirements: [REQ-28]

must_haves:
  truths:
    - "Plugin structure is compatible with npx skills add"
    - "README.md has skills.sh installation and registry submission guide"
    - "autoUpdate is configured and documented"
  artifacts:
    - path: "README.md"
      provides: "skills.sh section with npx install and submission guide"
      contains: "npx skills add alo-labs/topgun"
  key_links:
    - from: "README.md"
      to: ".claude-plugin/marketplace.json"
      via: "install instructions reference the same package identity"
      pattern: "alo-labs/topgun"
---

<objective>
Ensure TopGun is compatible with the skills.sh ecosystem (npx skills add), add skills.sh documentation to README.md, and verify autoUpdate configuration.

Purpose: Enable distribution via skills.sh as a secondary channel alongside the Claude plugin system.
Output: Updated README.md with skills.sh section, verified marketplace.json autoUpdate config
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-distribution-marketplace/07-01-SUMMARY.md
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify skills.sh compatibility and update README.md</name>
  <files>README.md</files>
  <action>
Read the existing README.md (created by 07-01). Replace the `<!-- skills.sh section added by 07-02 -->` placeholder with a full skills.sh section:

```markdown
## skills.sh Ecosystem

### Install via skills.sh

```
npx skills add alo-labs/topgun
```

### Compatibility

TopGun's `.claude-plugin/` structure is natively compatible with the skills.sh ecosystem. The `plugin.json` and `marketplace.json` files provide all metadata needed for discovery and installation.

### Registry Submission

To list TopGun on skills.sh:

1. Ensure the GitHub repository is public at `https://github.com/alo-labs/topgun`
2. Verify `plugin.json` and `marketplace.json` are in `.claude-plugin/`
3. Tag a release: `git tag v1.0.0 && git push origin v1.0.0`
4. Submit via: `npx skills submit alo-labs/topgun`

### Auto-Update

TopGun is configured with `autoUpdate.enabled: true` in marketplace.json. When a new GitHub release is tagged, installations will auto-update within 24 hours.
```

Also add a "Releases" section to README.md:

```markdown
## Releases

TopGun uses GitHub release tags for versioning. To create a release:

```bash
git tag -a v1.0.0 -m "TopGun v1.0.0 — Full Pipeline"
git push origin v1.0.0
```

The tag triggers autoUpdate for existing installations.
```

Per REQ-28: skills.sh is the second required distribution channel.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const r = fs.readFileSync('README.md','utf8'); const checks = ['npx skills add alo-labs/topgun','skills.sh','autoUpdate','Registry Submission','git tag v1.0.0']; const missing = checks.filter(c => !r.includes(c)); if(missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('README.md skills.sh section valid');"</automated>
  </verify>
  <done>README.md contains skills.sh install command, compatibility notes, registry submission guide, auto-update documentation, and release tagging instructions</done>
</task>

<task type="auto">
  <name>Task 2: Verify autoUpdate config and GitHub release readiness</name>
  <files>.claude-plugin/marketplace.json</files>
  <action>
Read `.claude-plugin/marketplace.json` and verify:

1. `autoUpdate.enabled` is `true`
2. `autoUpdate.channel` is `"stable"`
3. `version` field matches `plugin.json` version

If any field is missing or incorrect, fix it.

Then verify GitHub release readiness:
- Check if `v1.0.0` tag exists: `git tag -l "v1.0.0"`
- If no tag exists, do NOT create it (that's a release action, not a planning action) — but document in the summary that the tag needs to be created before distribution
- Verify `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` are both tracked by git

Per REQ-28: autoUpdate and GitHub release tags are required for distribution.
  </action>
  <verify>
    <automated>node -e "const m = require('./.claude-plugin/marketplace.json'); const p = require('./.claude-plugin/plugin.json'); if(!m.autoUpdate || !m.autoUpdate.enabled) { console.error('autoUpdate not enabled'); process.exit(1); } if(m.version !== p.version) { console.error('Version mismatch:', m.version, '!=', p.version); process.exit(1); } console.log('autoUpdate configured, versions match');"</automated>
  </verify>
  <done>autoUpdate enabled in marketplace.json, version matches plugin.json, GitHub release readiness documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skills.sh registry → user | Package metadata served by third-party registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | T (Tampering) | npx skills add | accept | skills.sh registry handles package integrity; TopGun relies on GitHub release tags for source of truth |
| T-07-04 | I (Information Disclosure) | README.md | accept | README is public documentation; no secrets included |
</threat_model>

<verification>
- README.md has complete skills.sh section with install, compatibility, submission, and auto-update docs
- marketplace.json autoUpdate.enabled is true
- marketplace.json version matches plugin.json version
</verification>

<success_criteria>
- README.md contains `npx skills add alo-labs/topgun` and registry submission guide
- autoUpdate configured and verified
- Version consistency between plugin.json and marketplace.json
</success_criteria>

<output>
After completion, create `.planning/phases/07-distribution-marketplace/07-02-SUMMARY.md`
</output>
