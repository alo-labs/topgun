---
phase: 04-secureskills-sentinel-audit-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/topgun-securer.md
  - skills/secure-skills/SKILL.md
autonomous: true
requirements: [REQ-11, REQ-15, NFR-01]

must_haves:
  truths:
    - "All external SKILL.md content is wrapped in structural envelope before any agent context injection"
    - "SKILL.md files containing curl, wget, or fetch in executable body sections are rejected with logged reason"
    - "Allowed-tools containing Bash, Computer, or wildcard are flagged before proceeding"
  artifacts:
    - path: "agents/topgun-securer.md"
      provides: "Structural envelope logic and pre-filter instructions"
      contains: "structural-envelope"
    - path: "skills/secure-skills/SKILL.md"
      provides: "Updated skill definition with envelope and pre-filter references"
  key_links:
    - from: "agents/topgun-securer.md"
      to: "topgun-tools.cjs sha256"
      via: "Bash tool invocation"
      pattern: "topgun-tools.cjs.*sha256"
---

<objective>
Implement the structural envelope wrapper and pre-filter checks for SKILL.md content in the SecureSkills agent.

Purpose: Ensures all external skill content is sanitized and wrapped before any security audit or agent context injection (REQ-11, REQ-15, NFR-01).
Output: Updated agents/topgun-securer.md with envelope + pre-filter logic sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/topgun-securer.md
@skills/secure-skills/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural Envelope + Phone-Home Pre-filter in topgun-securer.md</name>
  <files>agents/topgun-securer.md</files>
  <action>
Replace the stub implementation in agents/topgun-securer.md with the full SecureSkills agent instructions. Keep the existing frontmatter (name, description, model, color, tools) but add "Skill" to the tools array if not present.

Add these sections to the agent body, replacing the stub content:

## Step 1: Receive Skill Content
- Accept skill_name, skill_source, and raw SKILL.md content from orchestrator state
- Read content from the path provided in state (via `node "$CLAUDE_PLUGIN_ROOT/bin/topgun-tools.cjs" state-read skill_content_path`)

## Step 2: Pre-Filter Checks (REQ-15)
Before any envelope wrapping, scan the raw SKILL.md for disqualifying patterns:

**Phone-home detection:** Scan all lines between code fences (``` blocks) and lines starting with `- Bash(` or similar executable patterns. If any of these executable body sections contain the literal strings `curl `, `wget `, or `fetch(` (with word boundary — not inside comments or documentation prose), REJECT the skill immediately:
- Write rejection to state: `node "$CLAUDE_PLUGIN_ROOT/bin/topgun-tools.cjs" state-write audit_status "rejected"` and `state-write audit_rejection_reason "Phone-home pattern detected: {pattern} on line {N}"`
- Output `## SECURE REJECTED` marker and stop

**Allowed-tools inspection:** Parse the SKILL.md frontmatter `allowed-tools:` field. If it contains `Bash`, `Computer`, or `*` (wildcard), set a flag `has_dangerous_tools=true` and record the specific tools found. This flag is passed forward — it does NOT reject, but will trigger warnings at the approval gate (Phase 5).
- Write to state: `node "$CLAUDE_PLUGIN_ROOT/bin/topgun-tools.cjs" state-write skill_dangerous_tools "{tools_list}"`

## Step 3: Structural Envelope Application (REQ-11, NFR-01)
Wrap the SKILL.md content in the structural envelope. The envelope is a delimiter-based wrapper that prevents content injection:

```
<structural-envelope source="{skill_source}" name="{skill_name}" sha="{content_sha}">
{raw SKILL.md content — unchanged}
</structural-envelope>
```

Compute SHA-256 of the raw content BEFORE wrapping: `node "$CLAUDE_PLUGIN_ROOT/bin/topgun-tools.cjs" sha256 "$(cat {skill_path})"`

Store the enveloped content in a temp file and record path in state:
- `node "$CLAUDE_PLUGIN_ROOT/bin/topgun-tools.cjs" state-write enveloped_content_path "{path}"`
- `node "$CLAUDE_PLUGIN_ROOT/bin/topgun-tools.cjs" state-write content_sha "{sha}"`

IMPORTANT: The structural envelope section must be clearly marked so Plan 04-02 (Sentinel loop) can read the enveloped content path from state. This plan writes the "envelope preparation" section; Plan 04-02 writes the "Sentinel invocation" section.
  </action>
  <verify>
    <automated>grep -q "structural-envelope" agents/topgun-securer.md && grep -q "curl.*wget.*fetch" agents/topgun-securer.md && grep -q "Phone-home" agents/topgun-securer.md && grep -q "allowed-tools" agents/topgun-securer.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>agents/topgun-securer.md contains Step 1-3 with structural envelope wrapping, phone-home rejection logic (curl/wget/fetch in executable sections), and allowed-tools flagging. Stub content fully replaced.</done>
</task>

<task type="auto">
  <name>Task 2: Update skills/secure-skills/SKILL.md with envelope reference</name>
  <files>skills/secure-skills/SKILL.md</files>
  <action>
Update skills/secure-skills/SKILL.md to remove the stub marker and reflect that Phase 4 Plan 01 is implemented. Keep the existing frontmatter. Replace the body with:

# SecureSkills

**Status:** Phase 4 — Structural envelope and pre-filters implemented.

## Capabilities

- Wraps external SKILL.md content in `<structural-envelope>` tags with source attribution and SHA-256
- Pre-filters executable body sections for phone-home patterns (curl, wget, fetch) per REQ-15
- Inspects allowed-tools frontmatter for dangerous permissions (Bash, Computer, wildcard)
- Computes and stores content SHA-256 for downstream integrity verification

## Dispatch

This skill is dispatched by the TopGun orchestrator via the `topgun-securer` agent. Not normally invoked directly.

## Completion Markers

- `## SECURE COMPLETE` — audit passed
- `## SECURE REJECTED` — pre-filter rejection (phone-home detected)
- `## SECURE ESCALATED` — finding requires user decision
  </action>
  <verify>
    <automated>grep -q "Structural envelope" skills/secure-skills/SKILL.md && grep -q "SECURE REJECTED" skills/secure-skills/SKILL.md && ! grep -q "Stub" skills/secure-skills/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>skills/secure-skills/SKILL.md updated with capability list, dispatch info, and completion markers. No stub references remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external SKILL.md -> agent context | Untrusted skill content crosses into agent processing |
| SKILL.md frontmatter -> tools list | Untrusted metadata declares permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | SKILL.md content | mitigate | Structural envelope wraps content with source attribution and SHA-256; content cannot escape envelope delimiters |
| T-04-02 | Tampering | SKILL.md executable sections | mitigate | Pre-filter rejects curl/wget/fetch in executable body sections before any processing (REQ-15) |
| T-04-03 | Elevation of Privilege | allowed-tools wildcard | mitigate | Flag dangerous tools (Bash, Computer, *) for downstream approval gate warning |
</threat_model>

<verification>
- agents/topgun-securer.md contains structural envelope wrapping logic with `<structural-envelope>` tags
- Phone-home pre-filter checks for curl, wget, fetch in executable sections
- Allowed-tools inspection flags Bash, Computer, wildcard
- skills/secure-skills/SKILL.md updated with real capability descriptions
</verification>

<success_criteria>
- Structural envelope pattern documented and implementable in agent instructions
- Pre-filter logic covers all three phone-home patterns (REQ-15)
- Allowed-tools flagging records dangerous permissions to state
- No stub content remains in either file
</success_criteria>

<output>
After completion, create `.planning/phases/04-secureskills-sentinel-audit-loop/04-01-SUMMARY.md`
</output>
