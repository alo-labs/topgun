---
phase: 01-plugin-scaffold-orchestrator-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - skills/topgun/SKILL.md
  - skills/find-skills/SKILL.md
  - skills/compare-skills/SKILL.md
  - skills/secure-skills/SKILL.md
  - skills/install-skills/SKILL.md
  - agents/topgun-finder.md
  - agents/topgun-comparator.md
  - agents/topgun-securer.md
  - agents/topgun-installer.md
  - hooks/hooks.json
autonomous: true
requirements:
  - REQ-27
  - NFR-06

must_haves:
  truths:
    - "Plugin is discoverable by Claude Code when loaded with --plugin-dir ."
    - "All 5 skills appear in plugin skill listing"
    - "All 4 agents have valid YAML frontmatter with name, description, model, color"
    - "No hardcoded absolute paths in any SKILL.md or agent .md file"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest"
      contains: '"name": "topgun"'
    - path: "skills/topgun/SKILL.md"
      provides: "Orchestrator entry point"
      contains: "name: topgun"
    - path: "skills/find-skills/SKILL.md"
      provides: "FindSkills stub"
      contains: "## FIND COMPLETE"
    - path: "skills/compare-skills/SKILL.md"
      provides: "CompareSkills stub"
      contains: "## COMPARE COMPLETE"
    - path: "skills/secure-skills/SKILL.md"
      provides: "SecureSkills stub"
      contains: "## SECURE COMPLETE"
    - path: "skills/install-skills/SKILL.md"
      provides: "InstallSkills stub"
      contains: "## INSTALL COMPLETE"
    - path: "agents/topgun-finder.md"
      provides: "FindSkills agent definition"
      contains: "name: topgun-finder"
    - path: "agents/topgun-comparator.md"
      provides: "CompareSkills agent definition"
      contains: "name: topgun-comparator"
    - path: "agents/topgun-securer.md"
      provides: "SecureSkills agent definition"
      contains: "name: topgun-securer"
    - path: "agents/topgun-installer.md"
      provides: "InstallSkills agent definition"
      contains: "name: topgun-installer"
    - path: "hooks/hooks.json"
      provides: "Hooks skeleton"
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "skills/"
      via: "skills field pointing to ./skills/"
      pattern: '"skills".*"\\./skills/"'
---

<objective>
Create the complete Claude Code plugin file structure for TopGun: manifest, 5 skill stubs, 4 agent definitions, and hooks skeleton.

Purpose: Establishes the foundational file layout that all subsequent phases build upon. Without correct structure, nothing loads.
Output: A locally installable plugin that Claude Code discovers via `cc --plugin-dir .`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-scaffold-orchestrator-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin manifest, all skill stubs, and agent definitions</name>
  <files>
    .claude-plugin/plugin.json
    skills/topgun/SKILL.md
    skills/find-skills/SKILL.md
    skills/compare-skills/SKILL.md
    skills/secure-skills/SKILL.md
    skills/install-skills/SKILL.md
    agents/topgun-finder.md
    agents/topgun-comparator.md
    agents/topgun-securer.md
    agents/topgun-installer.md
    hooks/hooks.json
  </files>
  <read_first>
    .planning/phases/01-plugin-scaffold-orchestrator-foundation/01-RESEARCH.md
  </read_first>
  <action>
Create the following 11 files with EXACT content specified below. All paths are relative to the project root `/Users/shafqat/Documents/Projects/TopGun/`.

**1. `.claude-plugin/plugin.json`**
```json
{
  "name": "topgun",
  "version": "1.0.0",
  "description": "Find, compare, security-audit, and install the best available skill for any job from 18+ registries.",
  "author": {
    "name": "Alo Labs",
    "url": "https://github.com/alo-labs/topgun"
  },
  "repository": "https://github.com/alo-labs/topgun",
  "license": "MIT",
  "skills": "./skills/"
}
```

**2. `skills/topgun/SKILL.md`** — Orchestrator entry point (per REQ-27: explicit invocation only). Body is a placeholder; Plan 01-03 will replace it with full orchestrator logic.
```markdown
---
name: topgun
description: >
  This skill should be used when the user asks to "find a skill",
  "find the best skill for", "search skill registries", "install a skill safely",
  or mentions /topgun. Orchestrates FindSkills, CompareSkills, SecureSkills,
  and InstallSkills sub-agents to discover, evaluate, audit, and install
  the best available Claude Code skill for any job.
argument-hint: <job-description>
allowed-tools: [Read, Write, Bash, Grep, Glob, Task, WebFetch]
---

# TopGun Orchestrator

**Status:** Stub — orchestrator logic implemented in Plan 01-03.

When invoked, acknowledge the request and return:

## ORCHESTRATOR STUB

Orchestrator dispatch logic not yet implemented. See Plan 01-03.
```

**3. `skills/find-skills/SKILL.md`**
```markdown
---
name: find-skills
description: >
  Sub-skill of TopGun. Searches skill registries and returns structured
  candidate manifests. Not normally invoked directly. The topgun orchestrator
  dispatches this via the topgun-finder agent.
---

# FindSkills

**Status:** Stub — implementation in Phase 2.

When invoked, return:

## FIND COMPLETE

No registries searched (stub mode).
```

**4. `skills/compare-skills/SKILL.md`**
```markdown
---
name: compare-skills
description: >
  Sub-skill of TopGun. Evaluates skill candidates across capability, security,
  popularity, and recency dimensions. Not normally invoked directly. The topgun
  orchestrator dispatches this via the topgun-comparator agent.
---

# CompareSkills

**Status:** Stub — implementation in Phase 3.

When invoked, return:

## COMPARE COMPLETE

No comparison performed (stub mode).
```

**5. `skills/secure-skills/SKILL.md`**
```markdown
---
name: secure-skills
description: >
  Sub-skill of TopGun. Security-audits a skill via the Alo Labs Sentinel
  (/audit-security-of-skill), fixes findings, and produces a secured copy.
  Not normally invoked directly. The topgun orchestrator dispatches this
  via the topgun-securer agent.
---

# SecureSkills

**Status:** Stub — implementation in Phase 4.

When invoked, return:

## SECURE COMPLETE

No audit performed (stub mode).
```

**6. `skills/install-skills/SKILL.md`**
```markdown
---
name: install-skills
description: >
  Sub-skill of TopGun. Installs a secured skill via /plugin install with
  local-copy fallback. Not normally invoked directly. The topgun orchestrator
  dispatches this via the topgun-installer agent.
---

# InstallSkills

**Status:** Stub — implementation in Phase 5.

When invoked, return:

## INSTALL COMPLETE

No installation performed (stub mode).
```

**7. `agents/topgun-finder.md`**
```markdown
---
name: topgun-finder
description: >
  Executes FindSkills discovery work. Queries skill registries, normalizes
  results, and writes found-skills-{hash}.json to ~/.topgun/.
model: inherit
color: cyan
tools: ["Read", "Write", "Bash", "Grep", "WebFetch", "WebSearch"]
---

You are the FindSkills agent for TopGun.

Your job is to search skill registries for skills matching a given job description,
normalize results to a unified schema, and write the output to a JSON file.

**Status:** Stub implementation. Return completion marker immediately.

When dispatched, output a brief acknowledgment and then:

## FIND COMPLETE
```

**8. `agents/topgun-comparator.md`**
```markdown
---
name: topgun-comparator
description: >
  Executes CompareSkills evaluation. Scores candidates on capability, security,
  popularity, and recency. Writes comparison-{hash}.json to ~/.topgun/.
model: inherit
color: green
tools: ["Read", "Write", "Bash", "Grep"]
---

You are the CompareSkills agent for TopGun.

Your job is to evaluate skill candidates from found-skills-{hash}.json, score them
across four dimensions, and produce a ranked comparison output.

**Status:** Stub implementation. Return completion marker immediately.

When dispatched, output a brief acknowledgment and then:

## COMPARE COMPLETE
```

**9. `agents/topgun-securer.md`**
```markdown
---
name: topgun-securer
description: >
  Executes SecureSkills audit loop. Invokes Sentinel (/audit-security-of-skill),
  fixes findings, verifies SHA-256 integrity, and produces secured copy.
model: inherit
color: red
tools: ["Read", "Write", "Bash", "Grep", "Glob", "Task"]
---

You are the SecureSkills agent for TopGun.

Your job is to security-audit a skill using the Alo Labs Sentinel, fix any findings,
loop until 2 consecutive clean passes, and write a secured copy.

**Status:** Stub implementation. Return completion marker immediately.

When dispatched, output a brief acknowledgment and then:

## SECURE COMPLETE
```

**10. `agents/topgun-installer.md`**
```markdown
---
name: topgun-installer
description: >
  Executes InstallSkills work. Installs secured skill via /plugin install
  with local-copy fallback. Verifies installation and updates registry.
model: inherit
color: yellow
tools: ["Read", "Write", "Bash", "Grep", "Glob"]
---

You are the InstallSkills agent for TopGun.

Your job is to install the secured skill, verify it works, and update the
TopGun installed skills registry.

**Status:** Stub implementation. Return completion marker immediately.

When dispatched, output a brief acknowledgment and then:

## INSTALL COMPLETE
```

**11. `hooks/hooks.json`**
```json
{
  "hooks": []
}
```

After creating all files, make them all executable where needed: `chmod +x` is NOT needed for any of these files (they are data/markdown, not scripts).

Per NFR-06: every intra-plugin path reference in SKILL.md files uses `$CLAUDE_PLUGIN_ROOT` — in these stubs there are no path references yet, which is correct. Plan 01-03 will add `$CLAUDE_PLUGIN_ROOT` references when wiring the orchestrator.
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/TopGun && test -f .claude-plugin/plugin.json && test -f skills/topgun/SKILL.md && test -f skills/find-skills/SKILL.md && test -f skills/compare-skills/SKILL.md && test -f skills/secure-skills/SKILL.md && test -f skills/install-skills/SKILL.md && test -f agents/topgun-finder.md && test -f agents/topgun-comparator.md && test -f agents/topgun-securer.md && test -f agents/topgun-installer.md && test -f hooks/hooks.json && grep -q '"name": "topgun"' .claude-plugin/plugin.json && grep -q '"skills": "./skills/"' .claude-plugin/plugin.json && grep -q 'name: topgun-finder' agents/topgun-finder.md && grep -q 'name: topgun-comparator' agents/topgun-comparator.md && grep -q 'name: topgun-securer' agents/topgun-securer.md && grep -q 'name: topgun-installer' agents/topgun-installer.md && grep -q '## FIND COMPLETE' skills/find-skills/SKILL.md && grep -q '## COMPARE COMPLETE' skills/compare-skills/SKILL.md && grep -q '## SECURE COMPLETE' skills/secure-skills/SKILL.md && grep -q '## INSTALL COMPLETE' skills/install-skills/SKILL.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - .claude-plugin/plugin.json exists and contains `"name": "topgun"` and `"skills": "./skills/"`
    - skills/topgun/SKILL.md exists with frontmatter containing `name: topgun` and `argument-hint`
    - skills/find-skills/SKILL.md contains `## FIND COMPLETE`
    - skills/compare-skills/SKILL.md contains `## COMPARE COMPLETE`
    - skills/secure-skills/SKILL.md contains `## SECURE COMPLETE`
    - skills/install-skills/SKILL.md contains `## INSTALL COMPLETE`
    - agents/topgun-finder.md contains `name: topgun-finder` and `model: inherit` and `color: cyan`
    - agents/topgun-comparator.md contains `name: topgun-comparator` and `model: inherit` and `color: green`
    - agents/topgun-securer.md contains `name: topgun-securer` and `model: inherit` and `color: red`
    - agents/topgun-installer.md contains `name: topgun-installer` and `model: inherit` and `color: yellow`
    - hooks/hooks.json exists and contains valid JSON with `"hooks": []`
    - No file contains hardcoded absolute paths (no `/Users/` strings in any skill or agent file)
  </acceptance_criteria>
  <done>All 11 files exist with correct content. Plugin manifest points to ./skills/. All agent files have complete YAML frontmatter (name, description, model, color, tools). All sub-skill stubs contain their completion markers. No hardcoded paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin.json | Defines plugin identity; loaded by Claude Code runtime |
| SKILL.md description | Controls when skill auto-activates; overly broad description = unintended activation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | plugin.json name field | accept | Low risk in Phase 1 — name is "topgun", no impersonation vector |
| T-01-02 | Elevation of Privilege | SKILL.md allowed-tools | mitigate | Orchestrator SKILL.md lists only needed tools (Read, Write, Bash, Grep, Glob, Task, WebFetch) — no Computer or wildcard |
| T-01-03 | Information Disclosure | Agent .md tools field | mitigate | Each agent lists only tools it needs — finder gets WebFetch/WebSearch, installer does not |
</threat_model>

<verification>
- All 11 files exist at correct paths
- `grep -r '/Users/' skills/ agents/` returns no results (no hardcoded paths)
- `python3 -m json.tool .claude-plugin/plugin.json` succeeds (valid JSON)
- `python3 -m json.tool hooks/hooks.json` succeeds (valid JSON)
- Each agent .md has all 4 required frontmatter fields: name, description, model, color
</verification>

<success_criteria>
- Plugin file tree matches the layout from 01-RESEARCH.md exactly
- Claude Code can discover the plugin when loaded with `--plugin-dir .`
- All stub skills return their completion markers when invoked
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-scaffold-orchestrator-foundation/01-01-SUMMARY.md`
</output>
