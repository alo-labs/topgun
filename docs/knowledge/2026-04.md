# TopGun Project Knowledge — 2026-04

> Project-scoped intelligence for TopGun. Append-only within this month.
> Cap: 300 lines. Split to 2026-04-b.md if exceeded.

## Architecture Patterns

2026-04-18 — Registry dispatch pattern (mechanical subprocess fan-out): LLM-driven Agent fan-out is unreliable for guaranteed parallel dispatch — the model synthesizes plausible output from training data instead of spawning actual sub-calls. The reliable pattern is a Node.js binary (`topgun-tools.cjs dispatch-registries`) that uses `child_process.spawn` + `Promise.allSettled` to mechanically spawn one `claude --bare` subprocess per registry. The LLM calls this as a single Bash command; dispatch is entirely outside the model.

2026-04-18 — Partial-file aggregation pattern: each registry subprocess writes its own partial file (`registry-{hash}-{name}.json`) before the aggregator writes the combined artifact. Unavailable registries write `{ "status": "unavailable" }` rather than omitting their file. The aggregator always sees exactly N files, making failures visible and countable rather than silently absent.

2026-04-18 — Hook enforcement layering: three layers enforce the partial-count invariant in descending reliability — (1) prompt instruction (bypassable by model), (2) binary self-check via `validate-partials` command (bypassable if Bash call is omitted), (3) `PreToolUse:Write` hook in `~/.claude/settings.json` (not bypassable — OS-level interception). Only the hook provides a hard guarantee.

2026-04-25 — **Supersedes the 2026-04-18 mechanical subprocess pattern above.** `child_process.spawn('claude', ...)` cannot inherit OAuth session tokens — only environment-provided API keys are inheritable. Since OAuth is the Pro/Teams default, the v1.3 dispatcher silently broke FindSkills for the majority of users (issue #3). The v1.5+ pattern is in-process parallel `Task` dispatch from `topgun-finder` itself: one `Task` tool call per active registry emitted in a single assistant turn, each with `subagent_type: "general-purpose"`. `Task` sub-agents inherit the parent session's auth context, so OAuth and API-key both work. The runtime — not the model — guarantees the sub-calls actually execute, so the issue #2 reliability concern that drove v1.3 still does not return.

2026-04-25 — Hook enforcement now has only **two** layers (was three): prompt instruction + `PreToolUse:Write` hook. The intermediate binary self-check via `validate-partials` was retired alongside the subprocess dispatcher — the binary still exposes the command (still useful as a CLI debug check), but the canonical dispatch path no longer relies on it. The hook remains the only non-bypassable layer.

## Known Gotchas

2026-04-18 — Hook path is version-pinned: `validate-partials.sh` is referenced from the host runtime's hook configuration with its full absolute path, which includes the version string (e.g., `1.3.0`). On every plugin version bump the hook path must be refreshed manually, or automated via `topgun-tools.cjs init`.

2026-04-18 — Silver Bullet dev-cycle-check.sh blocks writes to plugin cache: runtime plugin cache directories such as `~/.codex/plugins/cache/` are blocked by a PostToolUse hook. Plugin changes must go through git commit → tag → GitHub release → user reinstall. Cannot directly sync the cache during development.

2026-04-18 — `--bare` flag on adapter subprocesses: the retired adapter subprocess path used `claude --bare` to skip hooks, LSP, and plugin sync for speed. Adapter prompt files still need to be fully self-contained because registry sub-agents should not depend on ambient project context.

2026-04-18 — Version alignment between plugin.json and marketplace.json: both files must carry identical version strings. Any mismatch causes Claude Desktop to not expose the plugin's skills or agents. Always bump both files together.

## Key Decisions

2026-04-18 — Node.js dispatcher over LLM Agent fan-out: chose mechanical subprocess fan-out because LLM-driven dispatch (even with `Agent` in the tools list) proved unreliable in production — the model synthesizes the expected output shape from training data rather than spawning sub-calls. Node.js subprocess fan-out is the only approach that provides dispatch guarantees. Closes issue #2.

2026-04-25 — **Revises the 2026-04-18 decision above.** Subprocess fan-out solves the dispatch-reliability problem but creates an auth problem: spawned `claude` processes do not inherit OAuth tokens. The `Task` tool is the right primitive — it is structured (the runtime, not the model, guarantees execution, fixing the issue #2 reliability concern) AND in-process (sub-agents inherit the parent's auth context, fixing issue #3). The decision is therefore: **structured `Task` dispatch, not LLM `Agent` fan-out, and not `child_process.spawn`.**

2026-04-18 — Unavailable partials always represented: if a registry task fails, times out, or does not produce a partial, aggregation records that registry as `status: "unavailable"` rather than silently omitting it. This ensures all 16 active registries are represented in `registries_searched`, making missing registries explicit and auditable rather than silently absent from results.

2026-04-18 — PreToolUse hook as final enforcement gate: prompt instructions and binary self-checks can both theoretically be bypassed by agent behavior (different mechanisms). The PreToolUse:Write hook intercepts at the OS tool-use level before any write lands on disk and cannot be bypassed by agent behavior.

2026-04-18 — SENTINEL bundled in plugin: bundling SENTINEL v2.3.0 directly in `skills/sentinel/SKILL.md` eliminates the external `/audit-security-of-skill` dependency. An external dependency could be unavailable, outdated, or itself compromised. Bundling version-locks audit quality with the plugin release.

## Recurring Patterns

2026-04-18 — Version-alignment pattern: `plugin.json` and `marketplace.json` must always carry identical version strings. The release checklist should verify both files before tagging. Mismatch is silent but breaks Claude Desktop skill/agent exposure entirely.

## Open Questions

*(none)*
